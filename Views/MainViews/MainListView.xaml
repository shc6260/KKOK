<UserControl x:Class="KKOK.Views.MainViews.MainListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KKOK.Views.MainViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <DataGrid IsReadOnly="True" BorderThickness="0" Background="White" RowHeaderWidth="0" CanUserSortColumns="False" SelectedItem="{Binding SelectedType}" ItemsSource="{Binding Items}" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn CanUserResize="False" HeaderStyle="{StaticResource DataGridHeaderStyle1}" Width="*" 
                                     Header="메뉴" Binding="{Binding Name}"
                                     ElementStyle="{StaticResource DataGridItemStyle}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
