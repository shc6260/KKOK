<Window x:Class="KKOK.Views.ScheduleViews.ScheduleModuleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KKOK.ViewModels.ViewModel.ScheduleViewModels"
        mc:Ignorable="d"
        Title="ScheduleModuleView" Height="450" Width="800">
    <Window.DataContext>
        <local:ScheduleModuleViewModel/>
    </Window.DataContext>

    <Grid>
        <DataGrid ItemsSource="{Binding Schedule, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}" AutoGenerateColumns="False" Margin="5" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="일정" Binding="{Binding TotalDate}"/>
                <DataGridTextColumn Header="담당자" Binding="{Binding ManagerName}"/>
                <DataGridTemplateColumn Header="소요기간">
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="일정내용" Binding="{Binding WorkName}"/>
                <DataGridTextColumn Header="해당업무번호" Binding="{Binding WorkNumber}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

    